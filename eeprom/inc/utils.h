#pragma once

#include <stdint.h>
#include <stdbool.h>

bool equalf(const float a, const float b);
float ftranspose(const float val, const float low1, const float high1, const float low2, const float high2);
